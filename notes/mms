method of manufactured solutions
- right quadrature formula?
- aky convergence rate tam ma byt?
- chcem aj delta solution (zobrazit skutocny error v paraview)
- four characteristics of chosen solution function

- najskor stvorcovy mesh len s kvapalinou
- najskor len nse ceknut, potom pridat teplotu

+ implementovat componnent ako enum, cize u_x, u_y, p namiesto 0, 1, 2
+ brat boundary conditions from mms ak je mms, nie z case filu. prip. case file pre mms, ktory urci, ze mms sa beru odjinud


ISSUE: constant H1, higher L2_velocity convergence rate
https://chatgpt.com/share/6a1861ff-5464-83eb-94ca-1f364b526f6d
- output also gradients (both analytical and computed)
- should the L2 for velocity have same slope as for pressure for NSE? What's the theory here?


PLAN:
- pozriet sa, ako to riesia v step-56 (riesenie o dvoch poliach)
+ vymysliet vhodne riesenie (divergence-free)
+ options for adaptive/global refinement in solver
+ implement global refinement
+ spocitat rhs with SymPy
+ implementovat triedy pre presne riesenie a rhs
+ BC na zaklade presneho riesenia (ako to treatuju v step-7?)
+ pocitanie erroru, ukladanie vysledkov (tiez nastudovat zo step-7)
- vyhodnotit a vyplotit convergence rates
+ moznost zbildit to bez mms
+ automatizacia? pridat manufactured solution do case-filu

codex: 
  + MMS classes are currently unused. The solver still applies case-file boundary conditions in
    src/conjugate_heat_transfer_solver.cc:419 and src/conjugate_heat_transfer_solver.cc:366.
  + NSE assembly has no manufactured force term added to local_rhs; it only assembles the residual
    of the unforced equation in src/conjugate_heat_transfer_solver.cc:583.
  - Temperature assembly builds only diffusion/advection matrix and zero RHS; it does not add
    TemperatureRightHandSide in src/conjugate_heat_transfer_solver.cc:662.
  + There is no MMS case/config switch. CaseConfig has no field like use_mms or problem_type in
    src/case_config.h:46.
  + There is no cases/mms.prm; only heat_exchanger.prm and pipe.prm exist.
  + No error computation against exact solution yet: missing VectorTools::integrate_difference /
    L2/H1 norms / pressure norm.
  - No convergence-rate table/plot automation.
  - No output of actual error fields for ParaView, which is also listed in your notes in notes/
    mms:4.