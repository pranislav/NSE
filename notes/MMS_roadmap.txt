MMS roadmap

+ conclude stage 1 (flow-only):
	x check error clustering - already done.
	+ report to Marc too regular pressure error
	+ try starting with finer mesh
	+ compiling problem with newest version
		+ check compilation
		+ update dealii
		+ check again
		+ report to Marc
	x Marc: react to upper bound vs rate (if we divide the equations, we are left just with the delta h to some exponent - is the exponent why is the "<=" there?)
	+ log cell (integral) error
	+ visualize cell error
	
stage 2 (add temp):
	+ add source terms to assemble
	+ boundary conditions handling
	+ compute error
	+ log error
	+ log cell error
	+ plotting workflow already automatized
	+ document the changes in readme
	+ test it
	- convergence analysis (maybe also error visualization) for two different Re and three different polynomial degrees

x stage 3 (solve on fluid/solid domain):
	x just easy flow channel
	-> would need extra manufactured solution and boundry care. Proper working of the multi-phase solver can be checked in cheaper ways
